"""
Optional framework integrations.

These modules import their hosting framework lazily so the base SDK
stays usable without pulling in LangChain / CrewAI / etc.
Install the extra you need:

    pip install agentguard-aegis[langchain]
"""
