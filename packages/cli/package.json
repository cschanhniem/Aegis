{
  "name": "@agentguard/cli",
  "version": "1.0.0",
  "description": "CLI for AgentGuard / AEGIS gateway",
  "bin": {
    "agentguard": "./dist/index.js",
    "aegis-mcp-proxy": "./dist/mcp-proxy.js",
    "aegis-http-proxy": "./dist/http-proxy.js"
  },
  "main": "./dist/index.js",
  "files": [
    "dist",
    "demo-agent",
    "repo-scanner",
    "codemod-inject"
  ],
  "scripts": {
    "build": "tsc && node ./scripts/bundle-demo-agent.mjs",
    "dev":   "ts-node src/index.ts",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "optionalDependencies": {
    "web-tree-sitter": "^0.20.8"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.3"
  }
}
